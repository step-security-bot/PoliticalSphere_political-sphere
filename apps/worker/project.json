{
  "name": "worker",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/worker/src",
  "projectType": "application",
  "tags": ["scope:worker", "type:service", "platform:node"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo \"No build pipeline for worker yet; skipping.\""
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run start:worker"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint apps/worker/src --ext .ts,.js --fix"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node --test apps/worker/tests/**/*.test.js"
      }
    }
  }
}
