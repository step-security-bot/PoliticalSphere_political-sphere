{
  "name": "auth-remote",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/auth-remote/src",
  "projectType": "application",
  "tags": ["app"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'Build remote app (copy Module Federation config into your builder)'"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'Serve remote app (configure webpack-dev-server with MF)'"
      }
    }
  }
}
